# Final — Capstone

A single, exam-style puzzle that ties the whole prep together. It's a
Task-1-flavoured problem (hash sets, first occurrence, boundary cases,
strict O(n)) wrapped in a story — exactly the shape a Codility task takes.

Treat it as a timed dry run: read the spec carefully, decide the data
structure, handle the empty/duplicate/perfect-sequence edge cases, and
confirm your complexity **before** peeking at `solution.js`.

| Exercise | What it drills |
|----------|----------------|
| [01-guardian-of-the-lost-artifact](01-guardian-of-the-lost-artifact) | `Set` presence + upward scan, pigeonhole bound, duplicates & empty input, O(n) |
