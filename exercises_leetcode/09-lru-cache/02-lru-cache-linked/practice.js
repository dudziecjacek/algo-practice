// Your turn. Implement the class, then run `npm test` (or `npm run test:watch`).
// Stuck? The reference answer is in ./solution.js
//
// Goal: O(1) get and put WITHOUT relying on Map insertion order.
// Use a hashmap (key -> node) + a doubly linked list with head/tail sentinels.

export class LRUCache {
  constructor(capacity) {}

  get(key) {}

  put(key, value) {}
}
